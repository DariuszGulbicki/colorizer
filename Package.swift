// swift-tools-version: 5.4

import PackageDescription

let package = Package(
    name: "Colorizer",
    products: [
        .library(
            name: "Colorizer",
            targets: ["Colorizer"]),
    ],
    dependencies: [
    ],
    targets: [
        .target(
            name: "Colorizer",
            dependencies: [])
    ]
)
